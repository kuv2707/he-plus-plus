func(str)println{
    #(str);
    #(`\n`);
}

func(k)abs{
   if(k>0)
   {
      return k;
   }else {
      return -k;
   }
}
if(3<5){

    0+println(abs(-5));
    0+println(`after`);
}
//not able to execute functions if they are not a part of an expression