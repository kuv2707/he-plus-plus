
funcion principal(a int, b boolean) {
    // definir a = 0, b=6.7*5+6*7
    // si a > 0 entonces {

    // } o si a < 0 entonces {

    // } o {
    //     definir h = 0
    // }
    // mientras que 2 + 3 < 5 && 5 == ++a {
    //     a = a + 1
    // }

    definir b = 3 > 5 ? a : b

    // para definir i = 0; i < 10; i = i + 1 {
    //     a = a + 1
    // }
    // devolver (a+brrrr(a))
}