funcion principal(a int, b boolean) void {
    // definir a = 0, b=6.7*5+6*7
    
    definir b = 3 > 5 ? a : b
    
}

funcion SpeechSynthesisVoice() void {
    si a > 0 entonces {
        
    } o si a < 0 entonces {
        
    } o {
        definir h = 0
    }
    mientras que 2 + 3 < 5 && 5 == ++a {
        a = a + 1
    }
}

definir a=0
definir b = 0
funcion bucle() int {
    definir a = 0
    para definir i = 0; i < 10; i = i + 1 {
        a = a + 1
    }
    devolver a+a-a
}